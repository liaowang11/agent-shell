name: Tests

on:
  workflow_dispatch:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: purcell/setup-emacs@master
        with:
          version: '29.1'

      - name: Clone dependencies
        run: |
          # The acp.el fork's initialize request accepts extra client
          # capabilities and _meta; agent-shell's AIR features test against it.
          git clone --depth 1 https://github.com/liaowang11/acp.el ../acp
          git clone --depth 1 https://github.com/xenodium/shell-maker ../shell-maker

      - name: Run tests
        env:
          EMACS_TEST_JUNIT_REPORT: '1'
        run: |
          emacs --batch -Q \
            -L . \
            -L ../acp \
            -L ../shell-maker \
            -L ../compat \
            --eval '(setq ert-load-file-name "test-results")' \
            --load tests/agent-shell-fakes.el \
            $(find tests -name '*-tests.el' | sort | sed 's/^/--load /') \
            -f ert-run-tests-batch-and-exit

      - name: Upload test results
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results.xml
